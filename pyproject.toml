[tool.poetry]
name = "better-web3"
version = "1.4.2"
description = "Chains, Contracts(Multicall, Disperse, ERC20/ERC721), batch calls and other.."
authors = ["Alen <alen.kimov@gmail.com>"]
readme = "README.md"
packages = [{include = "better_web3"}]

[tool.poetry.dependencies]
python = "^3.11"
web3 = "^6.6.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
